import { Router } from "express"

// Controllers


// Middlewares
import verifyToken from "../middleware/verify-token"

// Validates


const router = Router()

router.get("/", /* Started */)


export default router
